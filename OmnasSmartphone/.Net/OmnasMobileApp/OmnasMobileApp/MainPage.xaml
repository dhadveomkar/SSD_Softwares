<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="OmnasMobileApp.MainPage">

    <VerticalStackLayout Padding="30" Spacing="25">
        <Label Text="Omnas Mobile Monitor" FontSize="24" HorizontalOptions="Center" FontAttributes="Bold" />
        <Button Text="Refresh Inventory" Clicked="OnRefreshClicked" BackgroundColor="#38bdf8" />

        <CollectionView x:Name="DeviceList">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <HorizontalStackLayout Spacing="10" Padding="10">
                        <Label Text="📱" FontSize="20"/>
                        <Label Text="{Binding DeviceName}" FontSize="18" VerticalOptions="Center"/>
                    </HorizontalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>